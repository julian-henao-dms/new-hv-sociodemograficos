(function (window) {
  window.__env = window.__env || {};

  // API url
  window.__env.apiUrl = 'http://localhost:5005/api';
  //window.__env.apiUrl = 'https://localhost:44336/api';
  // Whether or not to enable debug mode
  // Setting this to false will disable console output
  window.__env.enableDebug = true;
  // window.__env.node = 'col';
  window.__env.company = 3;
  window.__env.regla = 159;

}(this));
